[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "fraud_spike_detector"
version = "0.1.0"
description = "Rust streaming fraud detection engine with Python bindings"
requires-python = ">=3.7"